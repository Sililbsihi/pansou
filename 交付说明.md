# 盘搜（pansou）交付说明书

> 交付日期：2026-09-25 ｜ 站长：Sililbsihi ｜ 版本 v1.1

## 一、你的网站资产

| 资产 | 地址/位置 |
|---|---|
| 线上网站 | https://pansou-lovat.vercel.app |
| GitHub 仓库 | https://github.com/Sililbsihi/pansou |
| Supabase 数据库 | supabase.com/dashboard/project/wnnmplfovdbbhymvmxdm |
| 数据源（Render 托管 PanSou） | https://pansou-api-1l59.onrender.com |
| Vercel 控制台 | vercel.com → chels-projects1 → pansou |

技术栈：Next.js 14 + Tailwind CSS + Supabase（Postgres + RLS）+ Render（PanSou 数据源）+ Vercel Cron

## 二、每天自动发生什么（无需任何操作）

- **04:00（北京时间）**：抓取任务——按关键词池轮转抓 24 个热词 + 近期热搜词的新资源，去重入库
- **05:00**：复检任务——轮流检测 40 条存量链接，更新「有效/失效」状态
- **追更加速器**：标题带「更新至/连载中」的剧，每天强制绕缓存抓最新集数
- **热搜反哺**：访客搜过的词自动进入抓取队列
- **失效反馈**：访客点「报失效」累计 3 次自动标记失效

## 三、手动操作（全部可选）

| 操作 | 方法 |
|---|---|
| 立刻抓取某关键词 | 网站搜该词 → 0 结果自动实时搜索 / 结果页点「⚡ 实时搜全网」 |
| 全库大补货 | 浏览器打开 `/api/cron/sync?secret=<CRON_SECRET>`（密钥在 Vercel 环境变量里） |
| 扩充关键词池 | 改 `lib/keywords.ts` → push 到 GitHub → 自动部署 |
| 接入新数据源 | Vercel 环境变量 `SOURCE_API_URLS` 追加地址（逗号分隔）→ Redeploy |

## 四、数据库表（Supabase）

| 表 | 用途 | 访客权限 |
|---|---|---|
| resources | 资源主表（标题/分类/网盘/链接/提取码/大小/状态） | 只读 |
| search_logs | 搜索记录（热搜榜数据源） | 只写 |
| reports | 失效反馈 | 只写 |
| sync_logs | 每日任务运行日志 | 不可见 |
| fetch_jobs | 即时抓取记录 | 不可见 |

搜索走数据库函数 `search_resources`（security definer）+ supabase-js 直查兜底双保险。

## 五、本地开发

```bash
git clone https://github.com/Sililbsihi/pansou && cd pansou
npm install
cp .env.example .env.local   # 填入 Supabase 等环境变量
npm run dev                  # http://localhost:3000
```

## 六、以后怎么迭代

1. 改代码（自己改，或让 AI 助手改——需要时生成一把新的 Fine-grained Token 给它推送，推完删除）
2. push 到 GitHub main 分支 → Vercel 自动构建部署（约 2 分钟）
3. 线上自动生效，数据库不动

## 七、已知限制（诚实清单）

1. **大陆访问**：`*.vercel.app` 域名在大陆网络被间歇性屏蔽。可选方案：A 保持现状（自用+代理用户）；B 花几块钱绑自定义域名（大陆直连，推荐）；C 迁移到国内可达托管（需改造）
2. **资源新鲜度边界**：系统能保证"全网有人分享，你最晚隔天收到"；但分享者永远比官方慢半拍，这是所有盘搜的物理边界
3. **Render 免费实例休眠**：闲置 15 分钟休眠，唤醒 20~50 秒——只影响抓取，不影响访客搜索体验
4. **数据源覆盖**：单一 PanSou 源对冷门内容可能无收录；接入更多源可扩展（架构已备好）
5. **蓝奏云链接稀少**：蓝奏云近年频繁封域名+限速，分享生态已迁移至夸克/UC，属行业现象

## 八、安全清单（已落实）

- ✅ 所有密钥只在 Vercel 环境变量 / 本地 .env.local，代码零硬编码（已扫描验证）
- ✅ .gitignore 排除 .env*（.env.example 模板除外）
- ✅ service_role 只在服务端；客户端无任何密钥
- ✅ 五张表全部启用 RLS，匿名只有明确放行的读/写
- ✅ 定时任务与管理接口带密钥鉴权（实测未授权请求被拒）
- ✅ 成人垃圾内容过滤（源黑名单 + 关键词黑名单）

## 九、待办（可选收尾）

- [ ] Vercel 环境变量加 `NEXT_PUBLIC_SITE_URL = https://pansou-lovat.vercel.app` → Redeploy（完善 SEO）
- [ ] 想要大陆访客直达 → 选定方案 B 或 C 后我来实施
- [ ] 以后需要新功能 → 随时找 AI 助手
